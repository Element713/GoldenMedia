// CalendarView.js
import React from 'react';

const CalendarView = ({ events }) => (
  <div className="card" style={{ maxWidth: 600, margin: '0 auto' }}>
    <h2>Calendar</h2>
    <p>Calendar view coming soon.</p>
    {/* TODO: Render events in a calendar grid */}
  </div>
);

export default CalendarView;
